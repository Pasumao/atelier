# 《AI 编程代理全权操作》特性结构与工具生态调研（第二轮）

> 调研时间：2025–2026（联网核实，四路并行）
> 与前作关系：本报告是 [`AI前端框架调研报告.md`](AI前端框架调研报告.md)（宏观定位与技术基线）的**深化篇**——回答"框架内部要长成什么样，AI 才能全权操作；AI 要用的工具/技能生态是什么样；人不碰代码怎么验收"。
> 原始素材：`research/05-ai-friendly-structure.md`（AI 友好结构）、`research/06-agent-tooling-ecosystem.md`（工具技能生态）、`research/07-agent-benchmarks-practices.md`（实证基准）、`research/08-human-boundary-loop.md`（人机边界）。
> 证据标注：**实证**=有可复现基准/对照；**经验**=社区工程共识；**未确认**=未核实。

> **状态注**：本文为第二轮调研快照。§7 的 5 个拍板点已由 `design-decisions.md` 决策 0-15 拍板（含决策 14 命名 Atelier）；本文"推荐实现优先级"已被决策 3/5/7/10 细化，阅读时以决策记录为准。

---

## 0. 结论先行（TL;DR）

**核心命题：让 AI 成功的不是"用了哪个框架"，而是「结构可推断性 + 约束可机器校验性」**（WebGen-V / SWE-WebDevBench / WebArena 实证支撑）。框架的"AI 友好" = **自描述（agent 自己能找到一切）+ 显式（零魔法）+ 可逆（改坏能回退）+ 可感知（过程与结果机器可见）**。

在"人类完全不碰内部代码"的约束下，框架天生分为三层：

| 层 | 回答的问题 | 核心结论 | 素材 |
|---|---|---|---|
| **结构层** | AI 如何理解并正确使用框架 | 文件小而多 + SSOT 注册表；类 HTML 声明式 + 扁平 schema；类型即契约即测试；四段式可行动错误；编译优先但保可读 | 05 |
| **工具层** | AI 用什么工具/技能操作框架 | MCP（已事实标准）+ SKILL.md（厂商无关技能格式）；**框架必须自带 MCP 工具集提供"查询/操作/审计"三类工具，并以 SKILL.md 包分发用法** | 06 |
| **边界层** | 人不碰代码时怎么验收 | 验收落在**结果层**（预览+断言+diff 报告），控制权用 stable-ID 写死，自治必须可逆+可停+可回放 | 08 |

**一句话架构**：框架 = 代码内核 + **机器可读契约层** + **内建代理层（MCP/Skills）** + 反馈通道 + **人对界面**（人只接触最后一层）。

---

## 1. 结构层：对 AI 最友好的特性（05 浓缩）

### 1.1 十原则 → 五组刚性设计约定

**A. 结构组织 —— 文件小而多 + 单一事实源**
- 薄而多文件（约 <400 行/文件）+ `AGENTS.md`/`llms.txt` 入口 + **SSOT 注册表**（组件/状态/主题显式登记为 JSON）；代理按需加载，避免为小改动读半仓。
- 组件/测试/故事/样式**同名同目录 co-located**：测试即文档即可执行契约。

**B. API 形态 —— 类 HTML 声明式 + 扁平 schema + 显式注册**
- 贴近模型预训练分布（HTML 近似）的模板/声明式层，优于命令式拼装（**实证**：WebGen-V 表明结构化表示优于自由文本；**经验**：Svelte 类 HTML 被认为最利 agent 推导）。
- 契约生成**扁平、无 `$ref`/`oneOf` 的简单 JSON Schema**（不同厂商对 JSON Schema 子集支持不一，Gemini 直接拒绝 `$ref`/`$defs`——复杂 schema 是结构化输出失败主因）。
- **禁隐式魔法**：显式导入/显式注册；禁隐式注入、隐式上下文、静默类型擦除——agent 出错最高发源。

**C. 类型系统 —— 类型即契约即测试**
- 强推 `discriminated unions` + `satisfies` + `exhaustive switch`：把错误从运行时前移编译期；**类型检查通过 = 硬性完成定义（DoD）**（**实证**：严格 TS 基准中 agent 在 `satisfies`/union 任务仍会作弊失误，类型错误是最可靠的自我纠错信号）。
- 类型错误文本必须自解释（一条错误定位到"改哪儿"，而非仅"类型不符"）。

**D. 错误与约束 —— 四段式可行动错误 + lint 即约束**
- 统一错误对象 `{code, message, context, fix}`：结构化、机器可解析、带示例与修复建议——报错是写给 agent 看的，不是给人看的。
- lint 规则即"规范化约束"，把"不该写什么"自动兜住（防 vibe 乱码、防漂移）。

**E. 编译与渲染 —— 编译优先但保可读 + 确定性**
- 编译时响应式换性能，但**保留生成代码可读/可审阅**（sourcemap + 原始语义入口），避免运行时黑盒（Svelte 5 runes 的教训：分析期生成代码可读性下降）。
- **hydration 默认确定性**；"不水合/部分水合"做成显式模式——SSR/hydration mismatch 是经典代理陷阱（TanStack 专门有修此的 PR；Qwik 以零水合规避）。

### 1.2 反馈回路（agent 迭代速度的生命线）

- **亚秒 HMR + 稳定 dev server**：确定性、无状态泄漏（Vite 比 Webpack 快 ~10x 的 HMR 是事实标准）。
- **headless 截图 diff 回环**：生成→渲染→截图→diff→断言，成为默认验收闭环；回归信号与 diff 必须被审阅，**不接受自动 accept 掩盖回归**（"测试通过但 agent 仍坏了"是通病）。
- **状态可检视**：checkpoint + time-travel + 经 MCP 检视 live app（先例：rn-devtools-mcp 让代理在 Cursor/Claude 里看组件/console/网络/Redux 状态）；细粒度响应式状态提供**值 + 依赖追踪检查器**（dartsignals/Preact DevTools 先例）。

---

## 2. 工具层：AI 编程工具与技能生态（06 浓缩）

### 2.1 生态现状

- **MCP 已是跨厂商事实标准**：官方 Registry 随 MCP 捐赠 Agentic AI Foundation（2025-11-25）；注册口径约 10,000+ 服务器、活跃同步口径约 5,033（两口径并存）。2025-11 规范新增 **OAuth 2.1 授权**（写权限需显式授权）、CIMD、XAA。
- **Agent Skills（SKILL.md，2025-10）是 MCP 之外的"技能层"，且更快被跨厂商接受**：一个 skill = 一个带 `SKILL.md` 的文件夹，**渐进式披露**（按需加载不占上下文）。厂商无关格式已事实成立（agentpack/codex-skills-alternative 社区拼装到 Cursor/Codex/Copilot），但统一规范无官方背书。
- **分工**：MCP 给"工具"，Skills 给"如何用"。

### 2.2 前端相关的 MVP 工具集

| 工具 | 能力 | 对框架的意义 |
|---|---|---|
| Playwright MCP | 浏览器自动化：点击/输入/断言/截图 | agent 的"手与眼睛"，回环标配 |
| Chrome DevTools MCP | 连真实浏览器调试：DOM/网络/性能 | 运行时检查面 |
| **Storybook MCP** | 组件清单/schema/故事暴露给 AI | **"框架自描述"的直接先例** |
| Figma MCP | 设计稿取样式/组件/变量，设计转代码 | 设计上下文喂给代理 |
| Supabase MCP / GitHub MCP | 数据层 schema 暴露；写权限需 OAuth | 后端与版本控制 |
| **Vercel MCP / Svelte AI docs** | 拉取/生成项目；`llms.txt`+MCP 暴露 API | **"框架官方自暴露"先例**（本项目必做项） |

### 2.3 协议层判断：**不押注任何单一协议**

AG-UI（CopilotKit）/ MCP-UI / A2UI（界面契约）、A2A/AGP（Google，代理间）、ACP（Zed，编辑器-代理）、ADP（IETF 草案）**分属不同层次、无胜者**。建议：框架对外只暴露 **自身 MCP 工具集 + 自描述 SKILL.md**；界面契约协议留作**可选适配层**（未来可接 AG-UI/A2UI），换生态主动权。

### 2.4 框架应内建代理工具清单（P0-P2）

| 优先级 | 内建能力 | 形态/先例 |
|---|---|---|
| **P0** | 组件注册表查询 | Storybook MCP |
| **P0** | 设计 token/主题查询（结构化 JSON） | Figma MCP、Svelte 主题变量 |
| **P0** | UI 状态快照 / 截图 | Playwright MCP、Warp 回环 |
| **P0** | diff / checkpoint / 回滚 | GitHub MCP、agentharness |
| **P0** | 测试运行器（单测+视觉回归） | Playwright MCP |
| **P1** | 运行时状态检查（响应式依赖图/渲染树） | Chrome DevTools MCP |
| **P1** | 文档/AGENTS.md/SKILL.md 自动生成 | Svelte AI docs |
| **P1** | 约束表达结构化（可 lint/type-check 而非自然语言） | schema-forge |
| **P2** | LLM 片段安全执行沙箱原语 | WebContainers/E2B、ArrowJS WASM |
| **P2** | 副作用审计（改动触及文件/资源/网络日志） | 权限模型、GitHub 审计 |

安全红线：写权限显式授权（OAuth 2.1）；**写文件 vs 执行 shell 分权**（脚本改文件能绕过逐条 diff，是已知安全缺口）；MCP 服务器供应链风险（registry 未经审查、skill 注入）列入企业级风险。

---

## 3. 实证与失败模式（07 浓缩）

### 3.1 实证结论（决定成败的是结构，不是框架名）

| 基准 | 关键结论 | 强度 |
|---|---|---|
| WebGen-V | **结构化表示**（分层设计/语义组件树）显著提升 LLM 网页生成质量 → 约束性 schema 优于自由文本 | 实证 |
| SWE-WebDevBench | 规约/脚手架/产物结构质量决定平台成败，而非模型单点 | 实证 |
| WebArena | 末端任务成功率仅 ~14-19%；**可达 DOM 结构清晰、语义化/可定位性强**显著影响 agent 命中率 | 实证 |
| SWE-bench 系 | 契约与测试可写性越高越可验证；**无测试/无类型 → "完成"不可判定** | 实证 |
| React/Vue/Svelte/HTMX 对比 | 无严格 A/B；主流经验：规则少、无虚拟 DOM、无魔法 DSL 更利 agent；HTMX 介入面最小最可验证 | 经验 |

### 3.2 五大失败模式 → 结构对策（这是设计约束的"负向清单"）

| 失败模式 | 根因 | 框架对策 |
|---|---|---|
| 布局/样式漂移 | 无语义令牌、硬编码颜色 | **语义化设计 token**（定义语义而非色值）+ 规范化样式系统 |
| 不可维护 | 无边界、无契约、隐式全局状态 | 类型即契约单源；单向声明式数据流；<400 行/文件 |
| 幻觉 API | 契约不可机器校验、黑盒依赖 | 强类型+lint 硬约束+编译即时反馈；schema 校验 |
| 无限修复循环 | 无验收判据 | spec 驱动：可机器断言的用例/快照/截图文凭作为完成判据 |
| 上下文爆炸 | 上下文/记忆无边界 | AGENTS.md 声明式上下文；模块化分层；显式 mem 边界 |

### 3.3 既有 AI-native 框架对照（共同模式 = 我们的设计共识）

ArrowJS 1.0（信号、无虚拟 DOM、纯类型、极简 API）、cedar-OS（状态感知 copilot 容器）、morph（stable-ID + View Transitions 活性仪表盘）、OpenTiny NEXT（web-agent 架构显式分层）。**共同模式**：① 类型/契约单源 ② 状态可检查可定位 ③ API/结构面小、反魔法 ④ 显式分层边界。**两路线分歧**：ArrowJS/cedar-OS 偏向"让 agent 生成**正确代码**"，morph 偏"让 agent 过程**可观察**"——**本项目应融合两条路线**。

另外两条铁律：**源码即库**（shadcn 模式：组件以可读源码进入项目，agent 可读改审；组件集成而非依赖）；**CLI 而非 IDE 是 agent 主界面**（声明式配置+可脚本命令=纯文本可 diff）。

---

## 4. 边界层：人在环外的验收回路（08 浓缩）

### 4.1 人机边界图（推荐）

```
人（意图 + 验收 + 边界）：① 写意图（自然语言/spec/验收清单）→ ② 定"done"（可执行断言+锁定区）
                            → ③ 看预览+diff 报告+断言报告拍板 → ④ 反馈（评论/点踩，沉淀为硬规则）
──────────────────────────────────────────────────────────────
机（实现 + 自治 + 自证）： ⑤ spec→plan→code（守锁定区）→ ⑥ 自我验证（lint/类型/单测/E2E/截图断言）
                            → ⑦ 可逆（每轮 checkpoint/分支/会话恢复）→ ⑧ 沙箱执行（preview 隔离）
                            → ⑨ 报告（diff+预览+断言+审计日志）→ ⑩ 阈值自治（越线自动停下请求人）
```

### 4.2 三原则

1. **验收落"结果层"而非"逐编辑层"**：人对最终产物的判断远准于逐条看 diff；仅高风险（首次生成/涉密钥/跨锁定区）临时下沉到 plan 门或 per-edit。
2. **控制权"写死"而非"靠 prompt 祈祷"**：人类锁定区用 stable-ID 显式声明；约束写为可校验机器规则（lint/类型/测试三夹逼）。
3. **自治必须"可逆 + 可停 + 可回放"**：无 checkpoint 就无人值守；命中漂移/成本/断言失败阈值**必须停下等人**，不得静默降级或无限重试。

### 4.3 业界最强的三件套 + 最该补位的位置

- 已成熟：**意图规格化（SDD，Jira/Cursor/Replit 已是主流）+ 预览部署（Vercel/Netlify preview）+ 自动验收（E2E/视觉回归联动 preview，Argos 等）**。
- **最弱（最值得框架补位）**：
  1. "意图即验收"——**可执行验收断言生成器**（Gherkin/结构断言自动化生成，尚无产品级落地）；
  2. 无人值守的**漂移/成本兜底**（框架级自动降级机制不存在）；
  3. **把代理改动当"可评论 diff"** 的协作层（Diffsmith/superset 先例）。
- **关键增量洞察**：把第一轮报告支柱 #3（可逆回放）、#5（三态模型）、#7（人机控制权）从"框架内部机制"**上升为面向人的产品功能**——可视化 diff 报告、一键回滚按钮、一键预览，这些是人可感知的验收界面。

---

## 5. 融合架构：框架总体形态

```
┌─ L5 人对界面（唯一人类接触面）─────────────────────────────────────┐
│ 意图规格模板 · 一键预览 · diff 报告 · 回滚按钮 · 点踩反馈 · 仪表盘      │
├─ L4 反馈通道 ─────────────────────────────────────────────────────┤
│ 亚秒 HMR dev server · headless 截图 diff 回环 · 状态检视 · 审计日志    │
├─ L3 代理层（内建 MCP Server + SKILL.md 包）─────────────────────────┤
│ 查询：组件注册表/设计 token/文档/状态快照                             │
│ 操作：diff/checkpoint/回滚 · 测试运行器 · 截图断言 · （P2）安全沙箱    │
├─ L2 契约层（机器可读单源）─────────────────────────────────────────┤
│ TS 类型 → 扁平 JSON Schema → 运行时校验 · 设计 token JSON · 注册表     │
├─ L1 代码内核 ─────────────────────────────────────────────────────┤
│ 信号内核 · 选择性编译器 · 类 HTML 声明式 · 流式原语(StreamedValue/     │
│ 增量JSON解析) · 三态状态模型(流式value+增量patch+乐观回滚) · 状态树/checkpoint│
└───────────────────────────────────────────────────────────────┘
  （协议层：AG-UI/A2UI 仅作可选适配器，不内嵌依赖）
```

---

## 6. 推荐实现优先级（更新 Phase 0/1）

| 优先级 | 交付项 | 依据 |
|---|---|---|
| **P0（v0.1）** | ① SSOT 注册表 + `AGENTS.md`/`llms.txt` 自动生成 ② 扁平 schema 契约工具链（TS→Schema→MCP，无 $ref/oneOf）③ 四段式错误对象 ④ 内建 MCP Server（查询面：组件/主题/文档）⑤ headless 截图 diff 验收闭环 ⑥ checkpoint/rollback | 05 原则 + 06 P0 清单 |
| **P1（v0.2）** | ⑦ 意图规格模板 + **可执行验收断言生成器** ⑧ 一键预览沙箱 ⑨ 人类可读 diff 报告 + 回滚按钮 ⑩ SKILL.md 包（框架"如何用"技能）⑪ 运行时状态检查 ⑫ 测试运行器接入 | 08 P0 + 06 P1 |
| **P2** | ⑬ stable-ID 锁定区 ⑭ 点踩反馈→硬规则 ⑮ 漂移/预算监测阈值自治 ⑯ 安全执行沙箱原语 ⑰ 界面契约适配层（AG-UI/A2UI） | 08 P2 + 06 P2 |

---

## 7. 需你拍板的关键设计决策

1. **代理层形态**：框架只对外暴露 MCP/Skills 供现有 agent（Claude Code/Cursor/Codex）使用，还是**内嵌一个框架自带 agent 运行时**（先例：Claude Agent SDK、Devin framework）？（建议：对外 MCP 必做；内嵌 SDK 作为 P2 可选）
2. **分发形态**：组件/约定是否"源码即库"（shadcn 式，项目内可读源码）？
3. **验收断言生成器**：由框架内置生成（需要框架自带 LLM 工具）还是由外部 agent 调用框架断言原语生成？
4. **沙箱默认**：浏览器内 WASM（WebContainers 式）还是云 VM（E2B 式）？框架默认"安全渲染"（只渲染声明的 UI 数据）还是"执行生成代码"？
5. **编译策略**：选择性编译范围，以及生成代码可读性保障（sourcemap+语义入口）。

---

## 8. 未确认事项（诚实清单）

- 无跨框架（React/Vue/Svelte/HTMX）同一 LLM 同任务的严格 A/B；"类 HTML 更利模型"为经验判断。
- 无统一"agent 可推导性"量化指标；文件大小"小而多"无受控对照实验。
- AI-native 框架（ArrowJS/morph 等）相对优劣无第三方验证；Svelte 生成代码可读性无定量指标。
- 视觉回归阈值/基线方法论无公认标准；框架级"安全执行沙箱"原语无先例。
- "意图→自动生成可执行验收脚本"无产品级落地；sleep/wake 一等原语、框架级漂移自动降级机制均不存在；批准粒度最优解无定论；无人值守实证收益缺数据。
- MCP Registry 服务器总数两口径并存；Figma 转码端到端质量未确认；SKILL.md 跨厂商统一规范无官方背书。

---

## 9. 下一步

1. 拍板 §7 的 5 个决策点（建议默认值已给出）。
2. 以其为输入起草 **Agentic DX 规范 v0.1**（可直接用 §1.1 五组约定作骨架）与**内建 MCP Server 工具清单 v0.1**（用 §2.4 P0 表）。
3. 启动 Phase 0 最小原型：契约工具链 + 注册表 + MCP 查询面 + 截图 diff 回环 + checkpoint——这五件是"AI 能全权操作"的地基。
