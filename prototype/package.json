{
  "name": "atelier-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Atelier Phase 0 最小可运行原型：信号内核 / 模板解释器 / 事务层 / 契约校验 / 注册表查询面（Vite dev）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
